var ballObject = {
  //radius
  //position
  //color
  //velocity
  //DOM element (document.querySelector("#ball"))
}

function constructBall() {
  // When this function runs, we should:
  // - set the following styles, according to ballObject
  //   - radius
  //   - position (starting)
  //   - color
}

function move() {
  setPosition();
  drawPosition();
}

function setPosition() {
  // when this function runs, we need to:
  //   - update the properties of the ballObject
  //   - run the drawPosition() function
}

function drawPosition() {
  // when this function runs, we need to:
  //   - use the properties of the ballObject to
  //     update the style of the ball DOM element
}
